// 名刺編集画面
import 'package:flutter/material.dart';

class CardEdit extends StatelessWidget {
  const CardEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('CardEdit');
  }
}
